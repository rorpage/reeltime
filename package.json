{
  "name": "reeltime",
  "version": "1.0.0",
  "description": "Reeltime — self-hosted continuous HLS video streamer with optional Director aggregator",
  "private": true,
  "scripts": {
    "generate": "npm install --prefix director --silent && node director/src/director.js generate director.config.yaml"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "Unlicense"
}
